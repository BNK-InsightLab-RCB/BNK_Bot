python-dotenv
pydantic-settings
qdrant-client
loguru

# PDF Parsing (MinerU replaced due to AGPL licensing constraints)
# - pdfplumber: deterministic lattice table extraction for born-digital,
#   ruled-table PDFs (primary path for BNK 설명서/약관)
# - docling: general/fallback parser for scanned or non-ruled layouts
# - rapidocr + onnxruntime: Korean OCR backend used by the Docling fallback
pdfplumber
PyMuPDF
docling
rapidocr
onnxruntime

# Embedding: KURE-v1 (Korean retrieval, BGE-M3 based, 1024-dim, 8192 ctx) — loaded
# via sentence-transformers; model weights (~2GB) auto-download to HF cache on first use.
sentence-transformers==5.5.1

# Add sLLM related libraries as they are identified
